<div class="container">
  <div class="row">
    <div class="col text-center">
      <%= link_to 'トレーニング開始', root_path, class: "text-center" %>

      <%= month_calendar events: @trainings do |date, training| %>
      <%= date %>

      <% training.each do |training| %>
      <div>

      </div>
      <% end %>
      <% end %>
    </div>
  </div>
</div>